mixin listButtonStatus(listButtonStatus)  
  .box-listButtonStatus
    each button in listButtonStatus
      button(
        class=`btn btn-sm ml-1 btn-outline-success ${button.class}`,
        button-status=`${button.status}`,
      ) #{button.name}